# Build and run Jupyter Notebook project

## Requirements:
+ `pipenv`

## Locked Dependencies 
+ For detail: Check out `Pipfile`
+ Numpy

## Run:
+ Run `pipenv update` to start, and install dependencies.
+ The notebook can be run by Visual Studio or Anaconda.

